# Notebook rules

- Keep notebooks thin. Core logic belongs in `src/` or `scripts/`.
- Clear outputs before committing.
- Use notebooks for visualization, debugging, or final demos.
- Save large generated images to Drive or ignored output folders.
